version: "3"
services:
  python3:
    build:
      context: .
      dockerfile: ./dockerfiles/python3.7/Dockerfile
    env_file:
      - .env
    volumes:
      - .:/src
    stdin_open: true
    tty: true
